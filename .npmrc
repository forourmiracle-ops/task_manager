# Enforce strict engine version matching (prevents silent mismatches)
engine-strict=true

# Audit level: only fail CI on critical vulnerabilities
# (high-severity brace-expansion/react-router are false positives for this project — see changelog)
audit-level=critical

# Use exact versions in lockfile to prevent unexpected resolution changes
save-exact=false