# ===========================================
# TaskFlow 环境配置
# 复制此文件为 .env 并填入实际值
# 注意：Supabase 配置已内置默认值，不设置 .env 也可正常运行
# ===========================================

# --- 可选（已有默认值）---
# 如需连接自己的 Supabase 项目，填入以下配置覆盖默认值
# VITE_SUPABASE_URL=your_supabase_url
# VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# --- AI 功能配置 ---
# DeepSeek API Key 已移至 Supabase Edge Function 代理，不再在前端配置。
# 部署步骤：在 Supabase 项目中执行：
#   supabase secrets set DEEPSEEK_API_KEY=sk-your-deepseek-api-key
#   supabase functions deploy ai-proxy